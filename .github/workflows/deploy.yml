name: Deploy Cloudflare

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Production deploy through GitHub Actions is disabled for demo mode. Connect the repository directly in Cloudflare."
